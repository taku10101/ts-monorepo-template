/**
 * Export all types for use in the application
 * Import these types in your components:
 * import type { User, Post, Comment } from "@/mock/types"
 */

export type { Comment, Post, User } from "../generators/example-generators"
export type { DatabaseSchema, MockConfig, MockGenerator } from "./base"
